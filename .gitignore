# OS / editor / tool cruft
.DS_Store
*.swp
.vscode/
.idea/
.claude/settings.local.json

# Reference materials (kept on disk, not in version control)
reference/

# LaTeX build artifacts (regenerated by the build)
*.aux
*.log
*.out
*.toc
*.fls
*.fdb_latexmk
*.synctex.gz

# Editor backups of .tex sources
*.bak
*.bak2
*.backup

# Exported conversation logs at the project root (unfiled)
/2026-*.txt
# …but transcripts archived into a project folder are kept in VCS.
!**/claude-code-transcripts/2026-*.txt

# Private working docs (kept on disk, not in version control).
# research-notes.md is intentionally tracked.
/docs/go-to-market.md
/docs/jadh-abstract.md
/docs/jadh-abstract-pdf/
